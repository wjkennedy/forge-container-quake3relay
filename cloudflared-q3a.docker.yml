# Example only. The actual Docker runtime config is generated by
# scripts/prepare-cloudflared-tunnel.sh into .cloudflared/config.yml.
tunnel: q3-websocket
credentials-file: /etc/cloudflared/credentials.json
protocol: http2

ingress:
  - hostname: q3a.a9group.net
    service: http://q3-relay:8080
  - service: http_status:404
